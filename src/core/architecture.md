# Architecture note

The **full, junior-friendly architecture guide** for this project lives at the repository root:

**[`architecture.md`](../../architecture.md)** (repository root)

Use that document for layer rules, dependency flow, and how to add features. This file stays here so anyone browsing `src/core/` knows where to look.
